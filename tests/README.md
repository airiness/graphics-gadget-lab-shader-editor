# Cross-package tests

Cross-package and end-to-end tests for the shader editor workspace.

Package-local tests live next to their package (for example
`packages/*/tests`); tests here exercise integration between two or more
workspace packages.
