/**
 * Presentation-layer components for the shader graph editor.
 *
 * Components render node definitions and ask the headless core's semantic
 * services for connection validity and resolved types; they never redefine
 * graph semantics themselves.
 */
export const editorUiPackageName = "@gglab/editor-ui";
