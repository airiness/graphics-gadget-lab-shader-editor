# @gglab/editor-ui

Presentation components for the GGLab shader graph editor.

Owns: canvas node/edge presentation, panels, inspector, and other UI
components.

Queries connection legality and resolved types through the headless core's
semantic services and never redefines graph semantics.
